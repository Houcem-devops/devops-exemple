#kind: pipeline
#name: default

#steps:
#- name: build-java-app
 # image: houcemserver/mydevops:latest
  #pull: if-not-exists
  #settings:
  #  goals: 
   # - clean 
    #- install 
kind: pipeline
type: docker
name: default

steps:
- name: test
  image: maven:3.9.3-sapmachine-11
  commands:
  - mvn install -DskipTests=true -Dmaven.javadoc.skip=true -B -V
  - mvn test -B
